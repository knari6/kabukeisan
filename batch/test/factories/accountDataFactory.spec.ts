import { describe, it } from "vitest";

describe("AccountDataFactory", () => {
  describe("build", () => {
    describe("パラメータがあるとき", () => {
      it("登録できること", () => {});
    });
    describe("パラメータがないとき", () => {
      it("登録できること", () => {});
    });
  });
});
