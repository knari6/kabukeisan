import { describe, it } from "vitest";

describe("CompanyFactory", () => {
  describe("create", () => {
    describe("パラメータがあるとき", () => {
      it("指定した内容で登録されること", () => []);
    });
    describe("パラメータがないとき", () => {
      it("randomな値で登録されること", () => {});
    });
  });
});
